name: Legacy Netflix Weekly Top 10 (Disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: echo "Legacy Netflix Telegram publisher moved to zzcsama/netflix-weekly-top10."
